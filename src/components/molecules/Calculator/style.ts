import {Box} from '@chakra-ui/react'
import styled from '@emotion/styled'

export const logo = styled(Box)`
  .logo {
    width: 300px;
    height: 100px;
  }
  .logoimg {
    max-width: 100%;
    max-height: 100%;
  }
`
